{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=sgcenario",
    "format": "prettier --write .",
    "lint": "prettier --check . && eslint .",
    "check": "turbo run check",
    "test:unit": "turbo run test:unit",
    "sync:lint-semver": "syncpack lint-semver-ranges",
    "sync:set-semver": "syncpack set-semver-ranges",
    "sync:list-mismatches": "syncpack list-mismatches",
    "sync:fix-mismatches": "syncpack fix-mismatches",
    "sync:update": "syncpack update --types '!local'"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "eslint": "^8.53.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^3.1.0",
    "prettier-plugin-svelte": "^3.1.0",
    "syncpack": "^11.2.1",
    "turbo": "^1.10.16",
    "vite": "^4.5.0"
  },
  "packageManager": "pnpm@8.6.10"
}
